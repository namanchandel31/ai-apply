/** Leaf module: realtime event names — no service imports. */

const EVENT_APPLICATION_UPDATED = "application.updated";
const CHANNEL_APPLICATIONS = "applications";

module.exports = {
  EVENT_APPLICATION_UPDATED,
  CHANNEL_APPLICATIONS,
};
