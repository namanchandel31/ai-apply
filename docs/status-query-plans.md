# Status query plans

Generated by `node scripts/explainStatusQueries.js`. Re-run after applying migration `013_status_poll_indexes.sql` and replace this file.

## Prerequisites

```bash
npm run migrate
node scripts/explainStatusQueries.js
```

## Queries analyzed

1. `status_snapshot` — legacy single-table read
2. `status_job_ai_process` / `status_job_send_email` — legacy N+1 job reads
3. `status_jobs_distinct_on` — alternative single job query
4. `status_bundle` — current production path (LATERAL joins)

## Acceptance

- No sequential scan on `applications` for `id + user_id` filter
- Job LATERAL arms use `idx_app_jobs_app_type_created` (Index Scan)

_Plan output will appear below after first local EXPLAIN run._
